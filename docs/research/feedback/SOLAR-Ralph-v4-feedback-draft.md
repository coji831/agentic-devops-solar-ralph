1. [§1.1 Setup] need to merge templates to destination file, rename doesnt work
2. [§1.1 Setup] 2 solar folder (solar_workflow, solar_system)
3. [§2.1 Architecture] workflow creaton to solar folder or github folder?
4. [§2.1 Architecture] no need deep pipeline, we can use workflow with difference of level, intertwine with each other (simplier and easier to spot gap and suggest user to add new flow, )
5. [§3.2 Workflow gaps + §9.1 User interaction] ask before proceed if not sure about the workflow to use before any work
6. [§3.2 Workflow gaps] hint user to update the flow when spotted gap (user question/require change mid/post task)
7. [§9.1 User interaction] use vscode/askQuestion to better understand user intent
8. [§12.2 Output quality + §3.1 Orchestrator - resolved by v4.2+v4.5, needs retest only] use haiku, better reasoning but low context windlow, required to compact session frequently → SOLUTION: v4.2 orchestrator delegation (minimize tool use) + v4.5 model selection (Sonnet for orchestrator) = naturally reduces compaction frequency. No separate implementation needed, just verify after v4.2+v4.5 complete.
9. [§3.3 Loop mechanism] orchestrator still dont know when to invoke a loop, need a mechanism to elevate a workflow to a loop to be tracked in ledger
10. [§2.1-2.3 Protocols + §3.3 Loop mechanism] ralph-loop mentioned but now we leave it trigger to the orchestrator, it must know when to trigger a loop, and how to track it in the ledger, we can use a flag to mark a workflow as loop, and when the flag is set, the orchestrator will track it in the ledger and invoke it when needed
11. [§1.1-1.6 Foundation + §3.4 Ledger format] orchestretor update the leger with wrong format (wrong session type) - need a harness for that, hooks will fire when in loop mode, so it must work as designed to prevent unwanted hook firing
12. [§5.11-5.14 Logs + §4.2 State management] need to implement a mechanism to prevent infinite loop, such as a counter or a timeout,
13. [§5.1 Memory] /memory/repo mentioned and orchestrator trying to save to it. we dont use it any more be cause its out side the repo, we need to investigate the memory further to have best use of it
14. [§11.1 Observability + §6.1 Agent tiers + §7.1.1 Docs quality] not seeing the curator ask for design or template when unsure, leading to many iteration in the loop (such mechanism was added to code implementor)
15. [§3.4 Ledger format] need to revert the leger to default before starting a new workflow, to prevent confusion and ensure accurate tracking of the new workflow
16. [§11.1 Observability|covered by #14] not seeing hooks writing to logs, need to implement a logging mechanism for hooks to better track their execution and debug issues
17. [§7.1 Docs process] need mechanism to identify when to update workflow or instruction, which docs to update, prevent duplicate or wrong position (eg. loop trigger rules was added to every workflow, but it should have it own instruction and will be trigger automatically when needed, instead of being added to every workflow)
18. [§8.1 Implementation] no control when the high level agent produce too many change request or too compicated change request, need a mechanism to control the number of change request and complexity, such as a threshold or a review process before executing the change request (premature optimization, we can start with logging the change request and complexity, and review it after a certain period to see if we need to implement a control mechanism)
19. [§7.1.3 Docs process] need dedicated docs reviewer to prevent overload for other reviewers
20. [§7.1.1 Docs quality] minimal docs writing, prefer accuracy and correctness over quantity, avoid overhead (implementation docs create too much code or unrealistic ACs)
21. [§2.1 Architecture] pipeline recognition working good for feature pipeline, promising to migrate to use workflows with similar concept
22. [§3.1 Orchestrator] unnecessary reading file when start a task, need a reader agent to gather infor rather than let the orchestrator read them all, open the door for using higher reasoning, larger context agent to orchestrator with out increasing cost - minimize all tool use of the orchestrator, and let it focus on orchestration, use other agents to gather information and provide it to the orchestrator when needed

23. [§7.1.2 Docs process] doc curator may leave the file in format error, the doc reviewer must ensure the format is correct before approving the doc, we can implement a pre-approval check for doc format to prevent this issue
24. [§7.2 Docs quality] wrong tech stack when design/create doc
25. [§6.1 Agent tiers + §8.1 Implementation] code implementor, doing extra work that not defined or defined wrong (check for test coverage and ask to install new dependency), should alway delegate back to the plan designing agent or orchestrator to make sure the work is necessary and correctly defined, we can implement a mechanism for code implementor to ask for clarification or approval before proceeding with the work, such as a review process or a question prompt to the orchestrator or plan designing agent.
26. [§10 Communication] steering with subAgent running in loop (possible with good ledger, so they can resume where they left off )
27. [§4.1 State management] need an agent for work breakdown and update the leger, the orchestrator can read that and decide what to do next, this must enforce a template when update the ledger to ensure consistency and accuracy, we can implement a work breakdown agent that takes the high-level plan and breaks it down into smaller tasks, and updates the ledger with the new tasks and their status, this will help the orchestrator to better track the progress and make informed decisions on what to do next.
28. [covered - distributed across v4.1] some new feature of v4 is not working,(log, learning, protocol) → #10 Inquiry gate (v4.1.1), #11 Learning capture (v4.1.2), #12 Session logging (v4.1.3), #14 Hook logging (v4.1.5)
29. [§1.1 Setup] some [FILL IN] are not filled after the settup
30. [§5.1 Memory] the orchestrator mention that they store learned lesson and other repo stuff in /memories/repo/ (copilot internal storage called repository memory), so we either use that or find a better way to enforce our solar folder. also what happen when user have the repo on another machine, or the repo is deleted, we need a more robust and reliable way to store the learned lesson and other important information, such as using a database or a cloud storage solution that can be accessed from anywhere and is not dependent on the local file system.
31. [positive feedback - not tracked] verification artifact working good
32. [positive feedback - not tracked] deliverable template working good while delegating
33. [§7.1.5 Docs structure] instruction update need to specific and split into small instruction, dont alway put everything in the main copilot_instructions
34. [§7.1.4 Docs process] template compliance need more enforcement, alway go for template first when crate/update or review
35. [§12.1 Output quality] agent explaining too much or generating too much, we need to make them more concise and to the point
36. [§12.2 Output quality] invoked agent are capped at the invoker's model (need further verification, but seems like the invoked agent is always using the same model as the invoker, which can lead to issues when the invoker is using a smaller model and the invoked agent needs a larger model to perform its task effectively, we need to implement a mechanism to allow the invoker to specify the model for the invoked agent, or to automatically select an appropriate model based on the task requirements)
37. [§6.2 Agent tiers] alway delegate to collector agent before plan design agent
38. [§3.1 Orchestrator] programmatic force init workflow, if not have workflow, ask user to pick or create new one, then start delegating, keep the context of the orchestrator from being flooded, all sub agent can comunicate via hand of template or ledger, and the orchestrator can read from them when needed, this will help to minimize the context size for the orchestrator and allow it to focus on orchestration rather than gathering information or making decisions on task breakdown.
39. [§6.1 Agent tiers] agent tiers: data gathering (collector) dont code, thinking or analyze straight to digging, only ask if need scope or output format, plan designing (planner) dont do broad reading, code, write excessive docs, only design, plaining for high level and review the work (its need to ask for report from other agent - if orchestrator was asked, it must delegate to proper agent), execution (implementor) no thinking straight t, review (reviewer),
40. [§8.2 Implementation] adaptation gap: for example, an feature require both frontend and backend changes need to match the design for both, if not the implementation will go off rail, similar to API consumption, if the API design is not well defined, the implementation will go off rail, we need to implement a mechanism to ensure the design is well defined and consistent across different parts of the system, such as a design review process or a design template that must be followed for all designs.
41. [§7.1.2 Docs process] files created with wrong format and directory, the implementor followed exactly what designed, but the design curator failed to document the design or the designer didnt have enough knowledge to design the feature properly, we need to implement a mechanism to ensure the design is well documented and accurate, such as a design review process or a design template that must be followed for all designs, and the doc curator must ensure the design is properly documented and accurate before approving the design.
42. [§2.1 Architecture] pipeline stages start notification working good, but need rewording or migrate to use workflow for finer grained control and better context management, we can implement a workflow for pipeline stages that includes the start notification as one of the steps, this will allow us to have better control over the flow of the pipeline and manage the context more effectively, especially for longer running pipelines that may require multiple steps and interactions with different agents.
43. [§5.1 Memory] the orchestrator add the learning to the ledger instead of memory or other proper docs
44. [§7.2 Docs quality] UI design not consistent with the current UI of the system, lack of UIdesign skill or it not loaded, or lack of verify/check the current UI before design
45. [§6.1 Agent tiers + §8.3 Implementation] implementor read a lot of files and do broad research before start coding, which increase the cost and time, also could pollute their context and cause hallucinations. We need to define better input/output for each agent and when/who to reach out when they need information, instead of letting them do broad research on their own, we can have a data gathering agent that is responsible for gathering all the necessary information and providing it to the implementor in a concise and organized manner, this will help to minimize the context size for the implementor and allow them to focus on coding rather than gathering information. (the current verification artifact is good, could use it as a data central for agents)
46. [§3.1 Orchestrator] once the orchestrator can be separated from doing task, can consider upgrade them to higher context window model
47. [§4.1 State management + §9.1 User interaction] need an indicator that the material was approved by user so next agent can pick up and start working on it, such as a status update in the ledger or a notification to the next agent, this will help to ensure that the workflow is progressing smoothly and that each agent knows when to start their work based on the approval of the previous step.
48. [§3.5 Context issues] conditional compacting context, the condition must be defined carefully to prevent context loss, this is to prevent the orchestrator from having to compact while in a chain of tasks that involve user feedback in between.
49. [§3.2 Workflow gaps] orchestrator actively check for user feedback to update instructions/workflow/memory
50. [§14.1 Technical bugs - v4.1.6 Day 5] terminal in background doesnt exit clearly → Solution: Update stop.cjs to track PIDs in ledger, kill on session end, 5s graceful shutdown + force-kill. NOTE: Incorrectly mapped as #51 in coverage map.
51. [§3.1 Orchestrator - FULLY COVERED in v4.2+v4.3] Extensive work on an feature (first cycle > fix bug > next cycle> fix bug > ...) can make the orchestrator lose track of the overall progress and goal, we need a mechanism to help the orchestrator to keep track of the overall progress and goal, such as a high-level summary or a progress tracker that is updated after each cycle of work, this will help the orchestrator to maintain a clear understanding of where they are in the process and what still needs to be done, even if there are multiple cycles of work and bug fixes. → SOLUTION (Research-Backed Only): v4.2.10 implements GSD-2's STATE.md ledger (Milestone→Slice→Task tracking) + Must-Have Verification (explicit acceptance criteria as checkable commands/file checks) + Reactive Stuck Detection (triggers when same tool+args repeats 3x without expected artifact, per GSD-2 + OpenDev doom-loop detection) + v4.3.9 implements OpenDev's System Reminders (context-aware nudges at decision points: incomplete todos, premature completion warnings) + GSD-2's Roadmap Reassessment (after each slice: re-evaluate, split/merge/reorder remaining work based on discovered dependencies). Research sources: GSD-2 framework (STATE.md ledger, stuck detection, Must-Haves, roadmap reassessment), OpenDev paper §2.3.4 (system reminders), §2.2.6 (doom-loop detection with fingerprint-based tracking).
